/**\n * tsconfig.json Best Practices\n * Configuración recomendada para TypeScript con SOLID principles\n */\n\nexport const TYPESCRIPT_BEST_PRACTICES = `\n# TypeScript Configuration para SOLID\n\n## Configuración Recomendada (tsconfig.json)\n\n{\n  \"compilerOptions\": {\n    // Strict Mode - Detecta más errores\n    \"strict\": true,\n    \"noImplicitAny\": true,\n    \"strictNullChecks\": true,\n    \"strictFunctionTypes\": true,\n    \"strictBindCallApply\": true,\n    \"strictPropertyInitialization\": true,\n    \"noImplicitThis\": true,\n    \"alwaysStrict\": true,\n\n    // No permitas cosas peligrosas\n    \"noImplicitReturns\": true,\n    \"noFallthroughCasesInSwitch\": true,\n    \"noImplicitUseStrict\": true,\n\n    // Módulos\n    \"module\": \"ESNext\",\n    \"target\": \"ES2020\",\n    \"lib\": [\"ES2020\", \"DOM\", \"DOM.Iterable\"],\n\n    // Caminos\n    \"baseUrl\": \".\",\n    \"paths\": {\n      \"@/*\": [\"./*\"]\n    },\n\n    // Output\n    \"declaration\": true,\n    \"declarationMap\": true,\n    \"sourceMap\": true,\n    \"outDir\": \"./dist\",\n\n    // Otras\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true,\n    \"forceConsistentCasingInFileNames\": true,\n    \"resolveJsonModule\": true,\n    \"moduleResolution\": \"node\"\n  },\n  \"include\": [\"**/*.ts\", \"**/*.tsx\"],\n  \"exclude\": [\"node_modules\", \".next\", \"dist\"]\n}\n\n## Reglas ESLint para SOLID\n\nExtender configuración con:\n\nrules:\n  # Single Responsibility\n  \"complexity\": [\"error\", 10]               # Complejidad máxima\n  \"max-lines\": [\"error\", 100]               # Líneas máximas por archivo\n  \"max-nested-callbacks\": [\"error\", 3]      # Anidamiento\n\n  # Interface Segregation\n  \"no-explicit-any\": \"error\"                # No uses 'any'\n  \"@typescript-eslint/explicit-types\": \"error\"  # Tipos explícitos\n\n  # Dependency Inversion\n  \"no-var\": \"error\"                         # Usa const/let\n  \"prefer-const\": \"error\"                   # Preferir const\n  \"@typescript-eslint/no-implicit-any\": \"error\"\n\n  # Code Quality\n  \"no-unused-vars\": \"warn\"                  # Variables no usadas\n  \"prefer-arrow-callback\": \"warn\"           # Arrow functions\n  \"object-shorthand\": \"warn\"                # Shorthand syntax\n` as const;\n\nexport const TYPE_PATTERNS = `\n# Patrones de Tipos Recomendados\n\n## 1. Tipos genéricos para componentes reutilizables\n\ninterface ComponentProps<T> {\n  data: T;\n  onUpdate: (data: T) => void;\n  render: (item: T) => React.ReactNode;\n}\n\n## 2. Tipos de utilidades (Utility Types)\n\n// Partial - todas las props opcionales\ntype PartialProps = Partial<ComponentProps>;\n\n// Pick - solo algunas props\ntype PickedProps = Pick<ComponentProps, \"data\" | \"onUpdate\">;\n\n// Omit - excluir algunas props\ntype OmittedProps = Omit<ComponentProps, \"render\">;\n\n// Record - objeto tipado\ntype StatusMap = Record<\"success\" | \"error\" | \"pending\", string>;\n\n// Readonly - inmutable\ntype ImmutableProps = Readonly<ComponentProps>;\n\n## 3. Tipos para mayor seguridad\n\n// Literal types para enums\ntype Status = \"pending\" | \"success\" | \"error\";\n\n// Const assertions\nconst MENU_ITEMS = [\n  { label: \"Home\", path: \"/\" },\n] as const;\ntype MenuItem = (typeof MENU_ITEMS)[number];\n\n## 4. Conditional types\n\ntype IsString<T> = T extends string ? true : false;\n\ntype Flatten<T> = T extends Array<infer U> ? U : T;\n` as const;\n\nexport const TESTING_PATTERNS = `\n# Patrones de Testing para SOLID\n\n## 1. Testing de tipos (compile-time)\n\nimport type { SectionRoute } from \"@/lib/types\";\n\n// Este código no compilará si no respeta la interfaz\nconst testRoute: SectionRoute = {\n  path: \"/test\",\n  title: \"Test\",\n  // Error: falta 'subtitle'\n};\n\n## 2. Testing de componentes\n\nimport { render, screen } from \"@testing-library/react\";\nimport { SectionScreen } from \"@/components/routes/SectionScreen\";\n\ntest(\"SectionScreen renderiza titulo y subtitle\", () => {\n  render(\n    <SectionScreen\n      title=\"Test Title\"\n      subtitle=\"Test Subtitle\"\n    />\n  );\n\n  expect(screen.getByText(\"Test Title\")).toBeInTheDocument();\n  expect(screen.getByText(\"Test Subtitle\")).toBeInTheDocument();\n});\n\n## 3. Testing de hooks\n\nimport { renderHook } from \"@testing-library/react\";\nimport { useNavigationConfig } from \"@/hooks/useNavigationConfig\";\n\ntest(\"useNavigationConfig retorna configuración por defecto\", () => {\n  const { result } = renderHook(() => useNavigationConfig());\n\n  expect(result.current.menuItems).toBeDefined();\n  expect(result.current.sectionRoutes).toBeDefined();\n});\n\ntest(\"useNavigationConfig acepta config custom\", () => {\n  const customItems = [{ label: \"Custom\", link: \"/custom\" }];\n  const { result } = renderHook(() =>\n    useNavigationConfig(customItems)\n  );\n\n  expect(result.current.menuItems).toEqual(customItems);\n});\n` as const;\n\nexport default {\n  TYPESCRIPT_BEST_PRACTICES,\n  TYPE_PATTERNS,\n  TESTING_PATTERNS,\n};\n